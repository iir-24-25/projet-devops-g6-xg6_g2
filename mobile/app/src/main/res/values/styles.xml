<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppButton" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Thème de base -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">        <!-- Couleurs principales -->
        <item name="colorPrimary">@color/primary_color</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent_color</item>
        <!-- Couleur du texte par défaut -->
        <item name="android:textColor">@color/text_color</item>
        <item name="android:windowBackground">@drawable/bg_gradient</item>
    </style>

    <!-- Style pour les titres (h1) -->
    <style name="TitleText">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/primary_dark</item>
    </style>

    <!-- Style pour les boutons principaux -->
    <style name="QuizButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">@color/primary_color</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">12dp</item>
        <item name="android:layout_marginTop">8dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>
    <!-- Style général de la page de résultats -->
    <style name="ResultBlockStyle">
        <item name="android:background">#ffffff</item>
        <item name="android:padding">20dp</item>
        <item name="android:layout_marginTop">30dp</item>
        <item name="android:elevation">4dp</item>
        <item name="android:radius">12dp</item>
    </style>

    <!-- Style du score -->
    <style name="ScoreTextStyle">
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">#333</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style pour les réponses correctes -->
    <style name="CorrectAnswerStyle">
        <item name="android:textColor">#2ecc71</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style pour les réponses incorrectes -->
    <style name="IncorrectAnswerStyle">
        <item name="android:textColor">#e74c3c</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style pour les corrections -->
    <style name="CorrectionStyle">
        <item name="android:fontStyle">italic</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

</resources>
